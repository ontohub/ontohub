- if can? :write, resource
  .pull-right
    .btn-group
      = link_to 'Edit', [:edit, resource], class: 'btn btn-default'
      = link_to 'Delete', resource, class: 'btn btn-danger', method: :delete, confirm: "Really delete the #{cls_sym} '#{resource}'?"
