.jumbotron
  .row
    .col-md-8
      %h1 Ontohub

      .features
        %p
          %strong Open
          \- based on open source software
        %p
          %strong Flexible
          \- combine different logics in the same #{Settings.OMS}
        %p
          %strong Distributed
          \- combine distinct #{Settings.OMS.pluralize} into a single one

      .numbers
        %p
          With
          = Ontology.accessible_by(current_user).count
          ontologies and
          = Repository.accessible_by(current_user).count
          repositories, continuously growing!

      %p
        = link_to 'Learn more', 'http://wiki.ontohub.org', class: 'btn btn-primary btn-lg', :target => "_blank"

    .col-md-4
      = render partial: 'shared/signin_or_signup'
