%nav.navbar.navbar-inverse.navbar-fixed-top{role: 'navigation'}
  .container
    .navbar-header
      = link_to Settings.name, :root, class: 'navbar-brand'

      %button.navbar-toggle{type: 'button', :'data-toggle' => 'collapse', :'data-target' => '.navbar-menu-collapse'}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar

    .collapse.navbar-collapse.navbar-menu-collapse
      %ul.nav.navbar-nav
        / For any visitor
        = menu_entry 'Repositories', :repositories
        = menu_entry 'Ontologies',   :ontologies
        = menu_entry 'Categories',   :categories
        = menu_entry 'Symbols',      :entities_search if !! Settings.display_symbols_tab
        = menu_entry 'Logics',       :logics
        = menu_entry 'Mappings',     :links

      = render partial: 'shared/session'
