-if !parent && can?(:write, cls_sym.to_s.classify)
  %td
    .btn-group
      = link_to 'Edit', [:edit, subject], class: 'btn btn-default btn-xs'
      = link_to 'Delete', subject, class: 'btn btn-danger btn-xs', method: :delete, confirm: "Really delete the #{cls_sym} '#{subject}'?"
