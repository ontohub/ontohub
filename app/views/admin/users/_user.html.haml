%tr
  %td= user.id
  %td= user.name
  %td= highlight user.email, params[:email_search]
  %td= user.admin?
  %td
    = link_to 'show', resource_path(user)
    = link_to 'edit', edit_resource_path(user)
    = link_to 'delete', resource_path(user), :confirm => 'really delete?', :method => :delete
