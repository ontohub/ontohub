%h2= @page_title = 'Users'

= render :partial => 'filter'

%p
  = pluralize(collection.total_count, 'User')
  found

= paginaton collection do
  %table
    %thead
      %th ID
      %th Name
      %th Email
      %th Admin
      %th
    %tbody
      = render collection
