= form_tag '', :method => :get, :class => 'ontology filter' do
  = text_field_tag :search, params[:search], :placeholder => 'Search for URI or name'
  = submit_tag 'Search', :name => nil, :disable_with => 'Searching ...'
