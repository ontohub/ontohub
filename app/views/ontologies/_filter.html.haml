= form_tag '', :method => :get, :class => 'ontology filter' do
  %span
    = text_field_tag :search, params[:search], :id => 'Ontology\'s uri or name', :placeholder => 'Ontology\'s uri or name'
  = submit_tag 'Search ontology', :name => nil, :disable_with => 'Searching ...', :title => "Search ontology"
