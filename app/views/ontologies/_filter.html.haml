= form_tag '', :method => :get, :class => 'filter ontology form-search' do
  = text_field_tag :search, params[:search], :placeholder => 'Search for URI or name'
  = submit_tag 'Search', :name => nil, 'data-disable-with' => 'Searching ...', :class => 'btn'
