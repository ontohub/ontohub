= form_tag '', :method => :get, :class => 'ontology filter' do
  = text_field_tag :search, params[:search], :placeholder => 'Ontology\'s iri or name'
  = submit_tag 'Search ontology', :name => nil, 'data-disable-with' => 'Searching ...', :title => 'Search ontology'
