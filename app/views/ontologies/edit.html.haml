= ontology_nav resource, :edit

= simple_form_for resource, url: resource_path(resource) do |f|
  = f.input :iri
  = f.input :name
  = render :partial => 'categories'
  = f.input :description
  = f.button :submit, disable_with: t(:wait), class: 'btn btn-primary'
