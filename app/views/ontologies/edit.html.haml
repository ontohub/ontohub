= ontology_nav resource, :edit

= semantic_form_for resource do |f|
  = f.inputs :iri, :logic, :name, :description
  = f.actions do
    = f.action :submit, :button_html => { :disable_with => t(:wait) }
