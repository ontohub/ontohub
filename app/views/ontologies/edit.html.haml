= ontology_nav resource, :edit

= semantic_form_for resource do |f|
  = f.inputs :iri, :name, :description
  = f.actions do
    = f.action :submit, :button_html => { 'data-disable-with' => t(:wait) }
