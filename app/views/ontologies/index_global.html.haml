%h2= @page_title = 'Ontologies'

%p
  = pluralize @count, 'ontology'
  currently available

.clearfix
:javascript
  var FiltersMap = #{@filters_map.to_json};
#OntologySearch{pagination: 'paginated', role: 'filter'}
