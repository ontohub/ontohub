- # lisiting of ontologies count by state
%ul
  - parent.ontologies.count_by_state.each do |entry|
    %li
      #{entry.count} #{entry.state}
      - if entry.state=='pending'
        = link_to 'Retry', [:retry_failed, parent, :ontologies], method: :post, class: 'btn btn-xs btn-primary'
