%h2= @page_title = 'Logics'

- if can? :create, Logic
  .pull-right
    = link_to 'Create Logic', :new_logic, class: 'btn btn-primary'

%p
  = pluralize(Logic.count, 'logic')
  currently available

= pagination do
  %table
    %thead
      %th Name
      %th IRI
      %th
      %th
    %tbody
      %tr
        %td (heterogeneous) Ontology Libraries
        %td
        %td= pluralize(Ontology.heterogeneous.count, 'Ontology Library')
        %td= pluralize(Ontology.heterogeneous_children.count, 'Child Ontology')
        %td
      = render collection
