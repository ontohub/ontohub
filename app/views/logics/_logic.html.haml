%tr
  %td= fancy_link logic
  %td= logic.iri
  %td= link_to |
        pluralize(logic.ontologies.size, 'Ontology'), |
        resource_path(logic, tab: 'ontologies') |
  %td
    = link_to |
      'with ' + |
      pluralize(Ontology.distributed_in(logic).size, 'Ontology Library'), |
      resource_path(logic, tab: 'distributed') |
