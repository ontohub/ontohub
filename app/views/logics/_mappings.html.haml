%h4 Mappings
%ol.latest
  - @mappings_from.each do |mapping|
    %li= fancy_link(mapping) {|e| e.to_s}
  - @mappings_to.each do |mapping|
    %li= fancy_link(mapping) {|e| e.to_s}
      
- if can? :update, Logic
  %ul.actions
    %li= link_to 'Add Mapping', new_logic_mapping_path
