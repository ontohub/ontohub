- if parent
  = repository_nav parent.repository, :ontologies

  = ontology_nav parent, :tasks
  
  %table.table
    %thead
      %tr
        %th License Model
        %th Description
    %tbody
      - @tasks.each do |task|
        %tr
          %td= task.name
          %td= task.description
