- if parent
  = repository_nav parent.repository, :ontologies, disable_actions: true

  = ontology_nav parent, :projects

  %h4 Add new Project to this Ontology
- else
  %h1 Add Project

= render 'form'
