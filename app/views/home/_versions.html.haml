%h3 Latest updates
%ul
  - @versions.each do |version|
    %li
      = link_to version.ontology, version.ontology
      by
      = link_to version.user, version.user
      = timestamp version
      ago