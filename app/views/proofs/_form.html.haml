= simple_form_for resource, url: form_url_chain do |f|
  = f.input :prover_ids, label: 'Provers', collection: ontology.current_version.provers, as: :check_boxes, hint: t('proofs.new.provers.none_is_default')
  = f.button :submit, t('proofs.new.send'), class: 'btn btn-primary'
