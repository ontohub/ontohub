%ul.breadcrumb
  -repository_breadcrumbs(@repository, params[:path],@is_head,@oid).each do |crumb|
    -if crumb[:last]
      %li.active
        =crumb[:name]
    -else
      %li
        =link_to crumb[:name], crumb[:path]
        %span.divider /
