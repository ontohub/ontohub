en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    
    creating: "Creating..."
    updating: 'Updating...'

    buttons:
      or: 'or'
      cancel: 'cancel'

    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"

    labels:
      defaults:
        uri: URI
        iri: IRI
      user:
        email: E-Mail
      ontology_version:
        raw_file: Source File
      logic:
        extension: File extension
        mimetype: MIME-Type
    hints:
      ontology_version:
        source_url: Please enter the URL to import the ontology from.
    placeholders:
      comment:
        text: Write a new comment
      metadatum:
        key: URI
        value: Value
